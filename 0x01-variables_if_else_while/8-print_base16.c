#include <stdio.h>

/**
 * main - entry point
 * Return: 0
 */
int main(void)
{
	int numb;
	char chr;

	for (n = 48; n <= 57; n++)
		putchar(n);
	for (chr = 'a'; chr <= 'f'; chr++)
		putchar(chr);
	putchar('\n');
	return (0);
}
